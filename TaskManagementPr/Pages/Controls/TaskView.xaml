<?xml version="1.0" encoding="utf-8" ?>
<Border
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:effectsView="clr-namespace:Syncfusion.Maui.Toolkit.EffectsView;assembly=Syncfusion.Maui.Toolkit"
    xmlns:pageModels="clr-namespace:TaskManagementPr.PageModels"
    xmlns:models="clr-namespace:TaskManagementPr.Models"
    xmlns:shimmer="clr-namespace:Syncfusion.Maui.Toolkit.Shimmer;assembly=Syncfusion.Maui.Toolkit"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Class="TaskManagementPr.Pages.Controls.TaskView"
    x:Name="TaskViewRoot"
    StrokeShape="RoundRectangle 20"
    Background="{AppThemeBinding Light={StaticResource LightSecondaryBackground}, Dark={StaticResource DarkSecondaryBackground}}"
    x:DataType="models:ProjectTask">

    <Border.Resources>
        <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
    </Border.Resources>

     <effectsView:SfEffectsView
        TouchDownEffects="Highlight"
        HighlightBackground="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}">
        <shimmer:SfShimmer
            BackgroundColor="Transparent"
            VerticalOptions="Fill"
            IsActive="{Binding IsBusy, Source={RelativeSource AncestorType={x:Type pageModels:IProjectTaskPageModel}}, x:DataType=pageModels:IProjectTaskPageModel}">
            <shimmer:SfShimmer.CustomView>
                <Grid
                    ColumnDefinitions="Auto,*"
                    Padding="{OnIdiom 15, Desktop=20}">
                    <BoxView
                        WidthRequest="24"
                        HeightRequest="24"
                        Margin="12, 0"
                        Style="{StaticResource ShimmerCustomViewStyle}"/>
                    <BoxView
                        Grid.Column="1"
                        HeightRequest="24"
                        Margin="12, 0"
                        Style="{StaticResource ShimmerCustomViewStyle}"/>
                </Grid>
            </shimmer:SfShimmer.CustomView>
            <shimmer:SfShimmer.Content>
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12" Padding="{OnIdiom 15, Desktop=20}">

                    <CheckBox Grid.Column="0"
                        WidthRequest="50"
                        HorizontalOptions="Start"
                        IsChecked="{Binding IsCompleted, Mode=OneTime}"
                        VerticalOptions="Center"
                        CheckedChanged="CheckBox_CheckedChanged"
                        AutomationProperties.IsInAccessibleTree="True"
                        SemanticProperties.Description="{Binding Title}"
                        IsVisible="{Binding ShowQuickCompleteButton, Source={x:Reference TaskViewRoot}, Converter={StaticResource InvertedBoolConverter}}" />

                    <Grid Grid.Column="1" SemanticProperties.Description="{Binding Title}">
                        <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                            <Label
                                Text="{Binding Title}"
                                HorizontalOptions="Start"
                                LineBreakMode="WordWrap" />
                            <Label
                                Text="{Binding Description}"
                                FontSize="13"
                                LineBreakMode="WordWrap"
                                IsVisible="{Binding HasDescription}"
                                TextColor="{AppThemeBinding Light=#444444,Dark=#d0d0d0}" />
                            <Label
                                Text="{Binding Subtitle}"
                                FontSize="12"
                                TextColor="{AppThemeBinding Light=#555555,Dark=#bbbbbb}"
                                LineBreakMode="WordWrap" />
                        </VerticalStackLayout>

                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding NavigateToTaskCommand, Source={RelativeSource AncestorType={x:Type pageModels:IProjectTaskPageModel}}, x:DataType=pageModels:IProjectTaskPageModel}"
                                CommandParameter="{Binding .}"/>
                        </Grid.GestureRecognizers>
                    </Grid>

                    <VerticalStackLayout
                        Grid.Column="2"
                        Spacing="4"
                        VerticalOptions="Center"
                        WidthRequest="100"
                        IsVisible="{Binding ShowQuickCompleteButton, Source={x:Reference TaskViewRoot}}">
                        <Button
                            Text="Выполнено"
                            Clicked="QuickComplete_OnClicked"
                            Padding="8,6"
                            FontSize="13"
                            IsVisible="{Binding IsCompleted, Converter={StaticResource InvertedBoolConverter}}" />
                        <Label
                            Text="Сделано"
                            FontSize="12"
                            HorizontalTextAlignment="Center"
                            TextColor="{AppThemeBinding Light=#2e7d32,Dark=#81c784}"
                            IsVisible="{Binding IsCompleted}" />
                    </VerticalStackLayout>
                </Grid>
            </shimmer:SfShimmer.Content>
        </shimmer:SfShimmer>
    </effectsView:SfEffectsView>
</Border>
